# Personal Claude Code config (permissions) — not shared. This pattern ignores the file both
# in this standalone project and while the folder is still nested inside its origin repo.
.claude/settings.local.json

# The shared engine pin (.claude/settings.json) is intentionally NOT ignored — it travels with
# the repo so every clone runs on the same model/effort.

# Node
/node_modules/

# Local runtime config (secrets/host-specific) — the committed template is config.example.json
/config.json

# Download-time secrets (Civitai API token) — the committed template is install/secrets.env.example
/install/secrets.env

# Local generated output from manual verification
/out.png

# cycle runtime
/.factory/
/cycle.log
PAUSE
